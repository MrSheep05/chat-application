import { StyledContainer, StyledMessage } from './styled';

const ConnectingMessage = () => {
  return (
    <StyledContainer>
      <StyledMessage>Connecting...</StyledMessage>
    </StyledContainer>
  );
};

export default ConnectingMessage;
